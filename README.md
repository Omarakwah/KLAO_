# Number Simplifier

This project simplifies large numbers and percentages in text using regex and LangChain.

## Setup
1. Clone the repository:
   ```bash
   git clone https://github.com/your-username/number-simplifier.git
  pip install -r requirements.txt
   python main.py